require 'rake'
require 'rake/testtask'
require 'rake/task'
require './MyApp'  #If you change the name of the main .rb file you need to update me.
Dir["#{File.dirname(__FILE__)}/lib/tasks/**/*.rake"].sort.each { |ext| load ext }